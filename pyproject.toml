[project]
name = "ecommerce-bot"
version = "0.1.0"
description = "An AI-powered conversational agent that answers product questions using RAG and autonomously processes orders through natural dialogue"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
